<%= simple_form_for([:admin, @usuario], validate: true, html: {class: 'form-horizontal'}) do |f| %>
  <%= error_messages_for @usuario %>

  <div class="form-inputs">
    <%= f.input :nome %>
    
    <%= f.input :email %>
    
    <%= f.input :password, label: 'Senha' %>
    
    <%= f.input :password, label: 'Confirme a Senha' %>

    <%= f.input :grupo, collection: [["Administrador", "admin"],["Usuário Comum", "usuario"]] %>
  </div>
  
  <div class="form-actions">
    <%= f.button :submit, class: 'btn-success' %>
  </div>
<% end %>
